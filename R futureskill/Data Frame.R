# Create the data frame.
student.data <- data.frame(
  student_id = c(1:5), 
  student_name = c("Rob","Mike","Tom","Tony","Gary"),
   score = c(62,54,77,72,85))

# Print the data frame.			
print(student.data) 
